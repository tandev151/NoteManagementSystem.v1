<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.notemanagement">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NoteManagement">
        <activity
            android:name=".NoteManagementActivity"
            android:label="@string/title_activity_note_management"
            android:theme="@style/Theme.Customer"></activity>

        <activity android:name=".SignInActivity"
            android:theme="@style/Theme.Customer"/>

        <activity android:name=".SignUpActivity"
            android:theme="@style/Theme.Customer" />

        <activity android:name=".MainActivity"
            android:theme="@style/SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>